To Use:
In the folder WhichShouldIDo in command line, type "javac *.java" to compile.
"cd .." to get right outside the file and then "java Main"
Set task list save by creating an empty text document somewhere and then, when prompted, paste/type the path to the text document + [document_name].txt

For example:
javac *.java
cd ..
java Main
Desktop/task_list.txt

